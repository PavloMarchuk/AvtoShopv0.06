@model IEnumerable<AvtoShop.WebUI.Models.ViewModelAvto>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Edit" class="btn btn-success">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.AvtoId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UserName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BrandName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ModelAvtoName)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.YearAvto)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Price)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.AvtoId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BrandName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModelAvtoName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.YearAvto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
			<td>
				@Html.ActionLink("Edit", "Edit", new { id = item.AvtoId }) |
				@Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
				@Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })|
				@Html.ActionLink("AddPhotos", "AddPhotos", "Photo",  new { id = item.AvtoId }, new { @class = "btn btn-primary" })|
				<a asp-controller="Photo" asp-action="AddPhotos" class="btn btn-primary">AddPhotos</a>
			</td>
        </tr>
}
    </tbody>
</table>

 
